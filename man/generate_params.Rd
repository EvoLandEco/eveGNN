% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_utils.R
\name{generate_params}
\alias{generate_params}
\title{Generate Random Parameters Based on Specified Distributions}
\usage{
generate_params(dist_info)
}
\arguments{
\item{dist_info}{A list of lists where each inner list contains information about the
distribution from which to generate random parameters. Each inner list must contain a
\code{distribution} field specifying the distribution type (one of "uniform", "normal",
"binomial", "poisson", "exponential"), an \code{n} field specifying the number of random
values to generate, and additional fields as required by the distribution type.
For example:
\itemize{
\item For Uniform: \code{min} (minimum value), \code{max} (maximum value)
\item For Normal: \code{mean} (mean value), \code{sd} (standard deviation)
\item For Binomial: \code{size} (number of trials), \code{prob} (probability of success)
\item For Poisson: \code{lambda} (rate parameter)
\item For Exponential: \code{rate} (rate parameter)
}}
}
\value{
A list of numeric vectors, where each vector contains the generated random values
from one of the specified distributions.
}
\description{
This function generates random parameters based on the distribution information provided.
Supported distributions are Uniform, Normal, Binomial, Poisson, and Exponential.
}
\examples{
dist_info <- list(
  list(distribution = "uniform", n = 10, min = 0, max = 1),
  list(distribution = "normal", n = 10, mean = 0, sd = 1),
  list(distribution = "binomial", n = 10, size = 1, prob = 0.5),
  list(distribution = "poisson", n = 10, lambda = 4),
  list(distribution = "exponential", n = 10, rate = 0.2)
)
random_params <- generate_params(dist_info)

}
